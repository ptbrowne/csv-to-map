{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MapConfig",
  "description": "Configuration for the points-map marimo notebook",
  "type": "object",
  "required": ["csv"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "csv": {
      "type": "string",
      "description": "Path to the CSV file (relative to this config file)"
    },
    "bbox": {
      "type": "string",
      "description": "Path to a GeoJSON file defining the map bounding box. Defaults to bbox.json next to the CSV. If absent the data extent is used."
    },
    "lat_col": {
      "type": "string",
      "default": "Latitude",
      "description": "Name of the latitude column in the CSV"
    },
    "lon_col": {
      "type": "string",
      "default": "Longitude",
      "description": "Name of the longitude column in the CSV"
    },
    "label_col": {
      "type": "string",
      "description": "Column whose values are used as point labels. Omit to use sequential numeric indices (1, 2, 3 …)."
    },
    "color_col": {
      "type": "string",
      "description": "Column whose values drive point colour. Requires color_map."
    },
    "color_map": {
      "type": "object",
      "description": "Maps color_col values to hex colours. Use '__default__' as a fallback for unmatched values.",
      "additionalProperties": {
        "type": "string",
        "pattern": "^#[0-9a-fA-F]{6}$"
      },
      "examples": [
        { "grassland": "#2ecc71", "__default__": "#e74c3c" }
      ]
    },
    "title": {
      "type": "string",
      "description": "Map title shown above the figure"
    },
    "style": {
      "type": "string",
      "default": "OpenStreetMap.Mapnik",
      "description": "Tile provider in dot notation. Any contextily provider works. Mapbox styles use Mapbox.<style-id> and require MAPBOX_TOKEN.",
      "examples": [
        "OpenStreetMap.Mapnik",
        "OpenStreetMap.HOT",
        "CartoDB.Positron",
        "CartoDB.DarkMatter",
        "CartoDB.Voyager",
        "Esri.WorldImagery",
        "Esri.WorldStreetMap",
        "Esri.WorldTopoMap",
        "Esri.WorldShadedRelief",
        "Esri.NatGeoWorldMap",
        "Stadia.AlidadeSmooth",
        "Stadia.AlidadeSmoothDark",
        "Stadia.AlidadeSatellite",
        "Stadia.OSMBright",
        "Stadia.Outdoors",
        "Stadia.StamenTerrain",
        "Stadia.StamenToner",
        "Stadia.StamenWatercolor",
        "OpenTopoMap",
        "CyclOSM",
        "Mapbox.streets-v12",
        "Mapbox.outdoors-v12",
        "Mapbox.light-v11",
        "Mapbox.dark-v11",
        "Mapbox.satellite-v9",
        "Mapbox.satellite-streets-v12"
      ]
    },
    "font_size": {
      "type": "number",
      "default": 8.5,
      "description": "Font size for point labels."
    },
    "circle_size": {
      "type": "number",
      "default": 6,
      "description": "Marker size for the circle drawn at each point."
    },
    "output": {
      "type": "string",
      "description": "Output PNG filename (relative to this config file). Omit to skip saving."
    }
  }
}
